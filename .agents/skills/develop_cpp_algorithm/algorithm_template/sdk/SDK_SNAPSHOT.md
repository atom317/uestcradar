# SDK Snapshot

This directory is a vendored, self-contained snapshot of `uestcradar/cpp/sdk/include`.

Snapshot contract: `frame-sdk-v1`.

It contains fixed POD and `header + std::vector payload` frame transport,
automatic frame codecs, `FrameAlgorithmAdapter`, and benchmark infrastructure
required by this template. Algorithm developers must not edit it.
